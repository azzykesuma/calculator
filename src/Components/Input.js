import React from 'react';
import './Input.css';

const Input = props => (
    <div className='input'>{props.input}</div>
)
// the function is using parentheses, not curly braces.
export default Input;